const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const quotes = await prisma.quote.findMany({ include: { boards: { include: { items: true } } } }); for (const q of quotes) { for (const b of q.boards) { let totalMaterial = 0; for(const i of b.items) { totalMaterial += Number(i.quantity) * Number(i.unitPrice); } if (totalMaterial > 8000 && totalMaterial < 10000) { console.log('Board:', b.id, 'Quote:', q.quoteNumber, 'Items Material:', totalMaterial); } } } } main().catch(console.error).finally(() => prisma.$disconnect());